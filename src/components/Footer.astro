---
const currentYear = new Date().getFullYear();
---

<footer
  class="relative py-40 px-8 bg-brand-bg border-t border-brand-border/30 overflow-hidden"
>
  <div class="max-w-9xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-20 mb-40">
      <!-- Brand HQ -->
      <div class="flex flex-col gap-10">
        <div class="flex items-center gap-3 group">
          <div
            class="w-12 h-12 flex items-center justify-center transition-all duration-700 overflow-hidden text-white group-hover:text-accent"
          >
            <svg
              viewBox="0 0 100 100"
              class="w-full h-full p-1 opacity-90 group-hover:opacity-100"
            >
              <rect x="5" y="5" width="90" height="90" fill="currentColor"
              ></rect>
              <path
                d="M25 30 L35 22 V68 L25 76 V30 Z M45 48 L55 40 V68 L45 76 V48 Z M65 30 L75 22 V68 L65 76 V30 Z"
                fill="black"
                transform="scale(1.1) translate(-4.5, -4.5)"></path>
            </svg>
          </div>
          <span class="text-3xl font-bold tracking-tighter text-white uppercase"
            >WEBKODE</span
          >
        </div>
        <p
          class="text-brand-muted text-sm leading-relaxed max-w-xs font-normal"
        >
          Arquitectura digital boutique enfocada en diseño de alto rendimiento y
          soluciones Web Core Vitals.
        </p>
      </div>

      <!-- Links Sections -->
      <div class="flex flex-col gap-8">
        <span
          class="text-[9px] font-black tracking-[0.4em] uppercase text-zinc-600"
          >Empresa</span
        >
        <nav class="flex flex-col gap-4">
          {
            [
              { name: "Servicios", href: "#servicios" },
              { name: "Portafolio", href: "#proyectos" },
              { name: "FAQ", href: "#faq" },
              { name: "Contacto", href: "#agendar" },
            ].map((link) => (
              <a
                href={link.href}
                class="text-sm text-brand-muted hover:text-accent transition-colors font-medium"
              >
                {link.name}
              </a>
            ))
          }
        </nav>
      </div>

      <div class="flex flex-col gap-8">
        <span
          class="text-[9px] font-black tracking-[0.4em] uppercase text-zinc-600"
          >Redes Sociales</span
        >
        <nav class="flex flex-col gap-4">
          {
            [
              {
                name: "LinkedIn",
                href: "https://www.linkedin.com/in/dmaurelc/",
              },
              {
                name: "Instagram",
                href: "https://www.instagram.com/dmaurel.cl/",
              },
              { name: "GitHub", href: "https://github.com/dmaurelc/" },
            ].map((link) => (
              <a
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                class="text-sm text-brand-muted hover:text-accent transition-colors font-medium"
              >
                {link.name}
              </a>
            ))
          }
        </nav>
      </div>
    </div>

    <!-- Final Branding -->
    <div class="relative pt-20 border-t border-brand-border/50">
      <div
        class="flex flex-col md:flex-row justify-between items-center gap-10 relative z-10"
      >
        <span
          class="text-[10px] font-black tracking-[0.4em] uppercase text-zinc-700"
          >©{currentYear} Webkode — Diseño Web</span
        >
        <div
          class="flex items-center gap-12 text-[10px] font-black tracking-[0.4em] uppercase text-zinc-700"
        >
          <span>Todos los derechos reservados</span>
          <span class="text-accent">v1.1.0</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Massive Ticker Background (Full Width) -->
  <div
    class="absolute bottom-0 left-0 w-full overflow-hidden pointer-events-none opacity-[0.08] select-none whitespace-nowrap pb-10 z-0"
  >
    <div class="flex animate-ticker-footer gap-10 items-center">
      <h2
        class="text-[20vw] font-black tracking-[-0.08em] leading-none uppercase text-white"
      >
        WEBKODE | WEBKODE | WEBKODE | WEBKODE |
      </h2>
      <h2
        class="text-[20vw] font-black tracking-[-0.08em] leading-none uppercase text-white"
      >
        WEBKODE | WEBKODE | WEBKODE | WEBKODE |
      </h2>
    </div>
  </div>
</footer>

<style>
  @keyframes ticker-footer {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  .animate-ticker-footer {
    animation: ticker-footer 160s linear infinite;
    display: flex;
    width: fit-content;
  }
</style>
