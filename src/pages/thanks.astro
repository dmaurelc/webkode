---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Gracias por contactarnos | Webkode">
  <main
    class="min-h-screen bg-black flex items-center justify-center px-4 relative overflow-hidden"
  >
    <!-- Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-accent/5 rounded-full blur-[120px]"
      >
      </div>
    </div>

    <div class="max-w-2xl w-full text-center relative z-10">
      <div class="mb-8 flex justify-center">
        <div
          class="w-16 h-16 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center animate-bounce-subtle"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="text-accent"
          >
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
        </div>
      </div>

      <h1
        class="text-5xl md:text-7xl font-bold text-white tracking-tighter mb-6 animate-fade-in-up"
      >
        ¡Mensaje <span class="text-accent">Recibido!</span>
      </h1>

      <p
        class="text-xl text-zinc-500 mb-12 max-w-lg mx-auto leading-relaxed animate-fade-in-up"
        style="animation-delay: 0.1s;"
      >
        Gracias por ponerte en contacto con nosotros. Hemos recibido tu
        solicitud y un arquitecto de software se pondrá en contacto contigo en
        las próximas 24 horas.
      </p>

      <div
        class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-up"
        style="animation-delay: 0.2s;"
      >
        <a
          href="/"
          class="h-14 px-10 bg-white text-black font-black text-xs uppercase tracking-widest flex items-center justify-center hover:bg-accent transition-colors duration-500"
        >
          Volver al Inicio
        </a>
      </div>
    </div>
  </main>
</Layout>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes bounce-subtle {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-bounce-subtle {
    animation: bounce-subtle 3s ease-in-out infinite;
  }
</style>
